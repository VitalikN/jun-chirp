import s from "./testimonials.module.scss";

const Testimonials = () => {
  return (
    <section className={s.section}>
      <div className={`${s.container}  `}>Testimonials</div>
    </section>
  );
};
export default Testimonials;
