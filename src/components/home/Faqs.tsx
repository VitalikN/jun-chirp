import s from "@/sass/layouts/faqs.module.scss";

const Faqs = () => {
  return (
    <section className={s.section}>
      <div className={`${s.container}  `}>Faqs</div>
    </section>
  );
};
export default Faqs;
