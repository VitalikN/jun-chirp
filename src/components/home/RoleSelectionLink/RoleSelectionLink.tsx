import s from "./roleSelectionLink.module.scss";
const RoleSelectionLink = () => {
  return (
    <section className={s.section}>
      <div className={`${s.container}  `}>RoleSelectionLink</div>
    </section>
  );
};
export default RoleSelectionLink;
