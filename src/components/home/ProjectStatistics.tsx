import s from "../sass/layouts/projectStatistics.module.scss";

const ProjectStatistics = () => {
  return (
    <section className={s.section}>
      <div className={`${s.container}  `}>ProjectStatistics</div>
    </section>
  );
};
export default ProjectStatistics;
